# HA Energy Forecast — AppDaemon pip requirements
# This file is informational only — the AppDaemon add-on does NOT read
# requirements.txt. Declare these via python_packages in the add-on's own
# Configuration tab instead; see ha_appdaemon_config.yaml / README.md.

pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
scikit-learn>=1.3.0

# lightgbm is optional — if it fails to install on your platform (e.g. armv7,
# which has no prebuilt PyPI wheel), the app automatically falls back to
# scikit-learn's GradientBoostingRegressor. Comment it out if it fails there.
lightgbm>=4.7.0,<5.0
holidays>=0.46
