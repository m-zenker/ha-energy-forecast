system_packages:
  - libgomp1
python_packages:
  - requests>=2.31.0
  - holidays>=0.46
  - pandas
  - numpy
  - scikit-learn
  - lightgbm>=4.7.0,<5.0
init_commands: []

# Without LightGBM (armv7 or no C compiler) — scikit-learn GBR fallback only.
# Keep libgomp1: scikit-learn requires it at runtime.
# system_packages:
#   - libgomp1
# python_packages:
#   - requests>=2.31.0
#   - holidays>=0.46
#   - pandas
#   - numpy
#   - scikit-learn
# init_commands: []

# Optional: AppDaemon MQTT plugin (required for mqtt_discovery: true)
# Add this to appdaemon/appdaemon.yaml:
#   plugins:
#     MQTT:
#       type: mqtt
#       namespace: mqtt
#       client_host: 192.168.1.x
#       client_port: 1883
#       client_user: YOUR_MQTT_USERNAME      # required if broker enforces auth
#       client_password: YOUR_MQTT_PASSWORD  # (Mosquitto add-on → Configuration → Logins)
