# apps.yaml may contain API credentials — keep a local copy only
apps/apps.yaml

.ipynb_checkpoints
*/.ipynb_checkpoints/*
__pycache__/
.env
.venv/
venv/
*.log
.pytest_cache/
*.egg-info/

# Model artifacts and runtime data (generated at runtime, never commit)
apps/energy_forecast/energy_model.pkl
apps/energy_forecast/energy_model.pkl.sha256
apps/energy_forecast/meta.pkl
apps/energy_forecast/meta.pkl.sha256
apps/energy_forecast/energy_history.csv

# HA database (large, local only)
*.db

# Temporary files / log drops
temp/

# Claude Code — local only, not for sharing
CLAUDE.md
MEMORY.md
